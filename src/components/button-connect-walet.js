const ButtonConnectWallet = ({ onClick }) => {
  return (
    <button onClick={onClick} className="cta-button connect-wallet-button">
      Conectar Carteira
    </button>
  );
};

export default ButtonConnectWallet;
